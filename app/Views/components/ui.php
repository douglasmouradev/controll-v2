<?php
declare(strict_types=1);

/**
 * Helpers de UI — use as classes CSS em app.css (.btn, .ui-card, .badge, .input, etc.)
 * Este arquivo documenta os padrões visuais do sistema.
 *
 * Botões:     btn btn-primary | btn-secondary | btn-danger | btn-ghost (+ btn-sm, btn-block)
 * Cards:      ui-card ui-card-body | ui-stat-card
 * Formulário: label + input | select | textarea
 * Badges:     badge badge-blue | badge-green | badge-yellow | badge-gray | badge-red
 * Tabelas:    data-table
 * Página:     page-header, page-title, page-subtitle
 * Filtros:    filter-bar
 */
